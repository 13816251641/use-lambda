package lambda.optional;

/**
 * @Auther ljn
 * @Date 2020/2/18
 * 感觉我们一般就用Optional.map即可,Optional.flatMap的使用
 * 场景是你的成员变量是用Optional<Car> optional包裹的才会
 * 使用,但是个人认为这样的场景比较少。
 */
public class UsageOfFlatMap {
}
